VITE_TMDB_API_KEY=fa982580ec81260e5e58a8ce852817de
VITE_API_BASE_URL=http://localhost:3001

# Optional: Use a TMDB v4 Bearer token instead of API key
# VITE_TMDB_BEARER=

# Non-Vite variables below are ignored by the frontend build but should live in the server/.env
# Keeping them here temporarily for parity; consider moving to server/.env and removing from frontend
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
BASE_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173
